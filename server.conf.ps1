# BN-BY Server Configuration
# Load by init.ps1 and deploy.ps1

# Server IP Address
$TargetIP = "8.218.123.248"

# SSH Username (usually 'root' or 'ubuntu')
$TargetUser = "root"

# SSH Port (default 22)
$TargetPort = 22
